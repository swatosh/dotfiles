# ~/.tmuxinator/bspot.yml

name: bspot
# pre_window: nvm use

# pre:

windows:
- rgs-vim:
    root: ~/Projects/rgs-token
    panes:
      - nvim
- rgs-cl:
    root: ~/Projects/rgs-token
    panes:
      -
- frgs-vim:
    root: ~/Projects/fusion-remote-gaming-server
    panes:
      - nvim
- frgs-cl:
    root: ~/Projects/fusion-remote-gaming-server
    panes:
      -




- nvim-config:
    root: ~/.config/nvim
    layout: even-horizontal
    panes:
      - editor: nvim
      - cl:

- dotfiles-config:
    root: ~/.dotfiles
    layout: even-horizontal
    panes:
      - editor: nvim
      - cl:

- zpretzo-config:
    root: ~/.zprezto
    layout: even-horizontal
    panes:
      - editor: nvim
      - cl:

